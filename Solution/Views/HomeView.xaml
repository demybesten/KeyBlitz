<UserControl x:Class="Solution.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Solution.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:HomeViewModel}">
    
    <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">HomeView</TextBlock>
        <Button
            Margin="0 20 0 0"
            Padding="5 2"
            HorizontalAlignment="Left"
            Command="{Binding NavigateNewTestCommand}"
            Content="Home" />
    </Grid>
</UserControl>